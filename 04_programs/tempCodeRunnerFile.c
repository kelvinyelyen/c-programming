    // // Loop through each digit of the decimal number
    // while (n != 0)
    // {
    //     remainder = n % 8;             // Get the last digit of the decimal number
    //     n /= 8;                        // Remove the last digit from the binary number
    //     oct += remainder * pow(10, i); // Add the value to the decimal number
    //     ++i;                           // Increment the power
    // }
    // printf("Octal number: %d \n", oct);

    // // Loop through each digit of the decimal number
    // i = 0;